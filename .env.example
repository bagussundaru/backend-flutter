# Server Configuration
PORT=5000
NODE_ENV=production

# Database Configuration (jika menggunakan database eksternal)
# DATABASE_URL=your_database_url_here

# Authentication (jika menggunakan autentikasi eksternal)
# AUTH_SECRET=your_auth_secret_here